import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class LosMegaPixeles extends StatelessWidget {
  const LosMegaPixeles({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Hoal'),
    );
  }
}
